@model IEnumerable<NTSIH.Models.mAuditoria>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.fecha)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.rol_id)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.modulo_id)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.sentencia)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.mPersona.identificacion)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.fecha)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.rol_id)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.modulo_id)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.sentencia)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.mPersona.identificacion)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.registro_id }) |
            @Html.ActionLink("Details", "Details", new { id=item.registro_id }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.registro_id })
        </td>
    </tr>
}

</table>
